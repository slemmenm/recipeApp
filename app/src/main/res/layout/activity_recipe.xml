<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:fillViewport="false">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="198dp"
            android:contentDescription="@string/recipe_image_description"
            android:src="@drawable/baseline_restaurant_menu_black_48dp" />

        <EditText
            android:id="@+id/recipe_view_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:layout_gravity="center"
            android:text="recipe title..."
            android:textSize="30dp"
            android:textStyle="bold"
            android:autofillHints="Add your recipe title here"
            android:inputType="text" />

        <EditText
            android:id="@+id/recipe_view_description"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="10dp"
            android:autofillHints="Add your recipe description here"
            android:gravity="center|top"
            android:inputType="textMultiLine|text"
            android:text="recipe description..." />
    </LinearLayout>
</ScrollView>